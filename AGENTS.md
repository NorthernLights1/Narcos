# Narcos

**Read `CLAUDE.md` in this same directory before doing anything else.** It is the
only orientation file for this repository and it applies to you.

This file exists so that agents looking for `AGENTS.md` are pointed at the same
instructions every other agent gets. Nothing project-specific is duplicated here,
so the two can never disagree. Put any change in `CLAUDE.md`.
