.venv/
__pycache__/
*.pyc
data/
media/
staticfiles/
.env
tools/
.pytest_cache/

# agent tooling (installed via `npx skills` / ECC installer, personal/local tooling)
.agents/
.claude/
skills-lock.json

# Round plans: real files here so Obsidian can see them; .claude/plans symlinks in
/plans/

# agent tooling mirrored for the codex CLI
.codex/
